/**
 * StirCraft Dark Mode Toggle
 * Handles theme switching between light and dark modes
 */

// Dark mode JS removed. No runtime logic here.
// Kept as neutral file to avoid 404s if referenced; remove file entirely if desired.
